name: Sheduling of task 
on: 
  schedule:
    - cron: '*/5 * * * *'
jobs:
  schedule:
    runs-on: ubuntu-latest
    steps:
    - name: Info
      run: |
        echo "This the second time im using actions"
